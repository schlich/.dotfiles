- shell:
  - [git clone --depth=1 https://github.com/romkatv/powerlevel10k.git ~/.powerlevel10k, Download Powerlevel10k]

- link:
  ~/.zshrc:
    force: true
    path: .zshrc

  ~/.p10k.zsh:
    path: powerlevel10k/.p10k.zsh

  ~/.local/share/fonts:
    path: powerlevel10k/.local/share/fonts/*

  ~/.config/nvim:
    path: neovim/.config/nvim/*
